% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validate_knn.R
\name{cross_validate_knn}
\alias{cross_validate_knn}
\title{Perform k-fold cross-validation for kNN}
\usage{
cross_validate_knn(train_x, train_y, k_values = seq(1, 21, 2))
}
\arguments{
\item{train_x}{Training predictors.}

\item{train_y}{Training response.}

\item{k_values}{Vector of k values to try. Defaults to odd values from 1 to 21.}
}
\value{
A data frame with k values and corresponding accuracy.
}
\description{
Perform k-fold cross-validation for kNN
}
\examples{
# Simulate small example dataset
set.seed(123)
df <- data.frame(
  buying = sample(1:4, 30, replace = TRUE),
  maint = sample(1:4, 30, replace = TRUE),
  persons = sample(2:5, 30, replace = TRUE),
  safety = factor(sample(c("low", "med", "high"), 30, replace = TRUE))
)

# Prepare inputs
train_x <- df[, c("buying", "maint", "persons")]
train_y <- df$safety

# Run kNN cross-validation
cross_validate_knn(train_x, train_y, k_values = c(1, 3, 5))
}
