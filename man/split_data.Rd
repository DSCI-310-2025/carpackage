% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_data.R
\name{split_data}
\alias{split_data}
\title{Split data into training and test sets}
\usage{
split_data(data, response_col = "safety", prop = 0.8)
}
\arguments{
\item{data}{The full prepared dataset.}

\item{response_col}{Name of the response column (as string).}

\item{prop}{Proportion of data to include in the training set.}
}
\value{
A list with train_x, train_y, test_x, test_y.
}
\description{
Split data into training and test sets
}
\examples{
df <- data.frame(
  feature1 = rnorm(100),
  feature2 = rnorm(100),
  safety = sample(c("low", "med", "high"), 100, replace = TRUE)
)
split <- split_data(df, response_col = "safety", prop = 0.75)
names(split)
# Returns: "train_x", "train_y", "test_x", "test_y"
}
